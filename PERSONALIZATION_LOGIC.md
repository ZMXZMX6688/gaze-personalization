# 个性化偏差—补偿—验证逻辑链

## 1. 研究问题

本项目不再把“适配后误差下降”直接等同于“实现了个性化”。研究对象首先定义为：

> 对用户 \(s\)，通用模型预测方向 \(\hat g\) 与真实方向 \(g\) 之间，在不同片段和注视方向上可重复出现、能够从校准集迁移到评测集的系统残差。

在 yaw/pitch 局部坐标中，将残差写为

\[
r_s(x,t)=b_s+D_s x+c_s(t)+\epsilon ,
\]

其中：

- \(b_s\)：与注视方向无关的用户固定偏置；
- \(D_sx\)：随预测方向变化的用户特异尺度、耦合或剪切；
- \(c_s(t)\)：时间、设备、姿态或场景变化引起的上下文漂移；
- \(\epsilon\)：不可约噪声。

只有 \(b_s\) 或 \(D_s\) 在隔离片段间稳定时，才属于可补偿的个性化偏差。若校准收益来自 \(c_s(t)\) 或采样覆盖差异，适配器只是拟合当前场景，不能解释为个性化。

## 2. 嵌套补偿模型与参数账本

三种适配器属于同一个由简到繁的几何补偿模型族。

| 假设 | 适配器 | 自由参数 | 参数含义 | 正交性 |
|---|---|---:|---|---|
| H0 | yaw/pitch bias | 2 | yaw 偏置、pitch 偏置，单位为度 | 不涉及线性矩阵；局部角坐标平移 |
| H1 | SO(3) rotation | 3 | 三维轴角向量 \((\omega_x,\omega_y,\omega_z)\) | 严格正交，\(R^TR=I,\det R=1\) |
| H2 | tangent affine | 6 | \(2\times2\) 线性矩阵的 4 个参数，加 2 个角偏置 | 不要求正交；允许尺度、剪切和 yaw/pitch 耦合 |

当前 benchmark 的具体边界为：

- bias：每个角偏置限制在 \([-10^\circ,10^\circ]\)；
- rotation：轴角向量范数限制为 \(10^\circ\)，Rodrigues 映射产生 SO(3) 矩阵；
- affine：\(A=I+\Delta\)，\(\Delta\) 的每个元素限制在 \([-0.25,0.25]\)，偏置限制在 \([-10^\circ,10^\circ]\)；
- 三种方法均使用 \(10^{-3}\) 的参数先验正则化；
- gate 从 \(\{0,0.25,0.5,0.75,1\}\) 选择整体参数缩放，内部验证收益不足 \(0.02^\circ\) 时回退为零。

必须区分两件事：SO(3) 的正交性只能保证补偿是合法刚体旋转，不能保证三个旋转参数在有限注视覆盖下可辨识；affine 的非正交性是表达尺度/剪切的必要能力，同时也是过拟合风险来源。

## 3. 完整研究链条

### 阶段 A：偏差存在性

先报告每位用户校准集和评测集的原始残差均值、残差方差以及方向覆盖。若用户残差均值与零相比不稳定，不进入补偿效果结论。

### 阶段 B：偏差稳定性

比较校准偏置与评测集 oracle 偏置，报告：

- bias drift：两个偏置向量的欧氏距离；
- prediction centroid shift：校准与评测注视中心差异；
- evaluation outside calibration box rate：评测方向落在校准方向包围盒之外的比例。

偏差漂移与方向覆盖变化必须作为效果的解释变量，而不是只报告最终平均误差。

### 阶段 C：补偿可辨识性

在完整校准池上分别拟合 H0/H1/H2，并报告：

- 参数数量及具体数值；
- 校准误差解释率；
- SO(3) 的 \(\|R^TR-I\|_F\)、\(\det R\) 和条件数；
- affine 的 \(\|A^TA-I\|_F\)、\(\det A\)、条件数、设计矩阵秩与条件数。

若参数达到边界、设计矩阵病态或不同抽样得到的参数方向不稳定，则不能声称识别出用户结构。

### 阶段 D：隔离评测

固定阶段 C 得到的参数，不使用 gate，在隔离评测集上直接验证。此步骤回答“识别到的偏差是否可迁移”。gate 的结果只回答“工程上能否避免错误补偿”，不能作为适配器机制成立的证据。

### 阶段 E：门控部署

只有阶段 A–D 通过后，才比较 calibration-validation gate 的工程收益。最终必须同时报告：

1. 无补偿 baseline；
2. 无门控机制补偿；
3. 带门控部署结果；
4. gate 激活率和回退率。

## 4. 现有五折数据的机制诊断

诊断使用 55 位具有可行校准/评测划分的用户、每位用户完整校准池，并关闭 gate 和正则边界，以暴露模型自身的可辨识性。真实输出位于：

`/data1/luxliang/gaze-personalization/cv5/aggregate/mechanism-analysis`

| 协议 | 模型 | 校准解释率 | 评测平均 Gain | 评测中位 Gain | 改善用户比例 |
|---|---|---:|---:|---:|---:|
| chronological | bias | 14.19% | +0.0504° | +0.0424° | 67.27% |
| chronological | SO(3) | 16.81% | -0.1145° | +0.0288° | 58.18% |
| chronological | affine | 32.00% | -0.1958° | -0.0257° | 45.45% |
| interleaved | bias | 14.66% | +0.0380° | +0.0208° | 65.45% |
| interleaved | SO(3) | 16.98% | -0.1273° | +0.0013° | 50.91% |
| interleaved | affine | 31.36% | -0.1814° | -0.0056° | 47.27% |

主要结论不是“affine 最好”，而是：

1. 固定偏置是当前唯一获得正向平均迁移证据的机制。
2. SO(3) 始终满足几何正交性，但几何合法不等于统计可辨识。
3. affine 在校准集解释最多，却在评测集退化，证明额外尺度/剪切主要拟合了校准场景。
4. 当前 benchmark 中 affine 的正向结果依赖正则、参数截断和 gate；这应解释为安全部署效果，不能反推 6 个个性化参数均具有真实含义。

典型失败用户 `NVIDIAVR_10_1` 的评测方向 100% 位于校准方向包围盒之外，注视中心偏移 17.13°。无约束 SO(3) 拟合出 29.53° 旋转并使评测退化 7.12°；affine 设计矩阵条件数达到 614.96，并使评测退化 7.30°。这是方向覆盖不足导致的不可辨识与外推失败，不是“用户需要更复杂适配器”。

## 5. 后续实验的准入规则

后续不再平铺更多适配器。实验按以下顺序推进：

1. 对 bias 做跨片段参数稳定性和置信区间分析；
2. 按方向覆盖和 bias drift 分层，确认收益是否来自稳定个体偏置；
3. 只有当 bias 后残差仍与方向显著相关，且 affine 参数跨抽样稳定，才启用 H2；
4. SO(3) 只有在三维方向覆盖满足可辨识条件且旋转参数跨片段一致时保留；
5. 换设备、姿态或时间场景时，先重新做阶段 A/B，不直接沿用旧适配器。

主结论应围绕“偏差类型及其稳定边界”，适配器效果只是该逻辑链的末端验证。

## 6. 偏置稳定性与可靠性收缩

对校准样本按时间交错分成两半，分别估计偏置 \(b^{(1)}\) 与 \(b^{(2)}\)。定义参数不稳定度

\[
d=\|b^{(1)}-b^{(2)}\|_2 .
\]

两半估计差的方差是单个半样本估计方差的两倍，而全样本均值的估计方差又是半样本的二分之一，因此以 \(d^2/4\) 估计全样本偏置噪声能量。可靠性缩放为

\[
\alpha=\mathrm{clip}\left(1-\frac{d^2}{4\|\hat b\|_2^2},0,1\right).
\]

最终补偿参数为 \(\alpha\hat b\)。该过程仍然只有两个用户参数；\(\alpha\) 是由校准集内部重复性计算的估计可靠度，不是新增可训练参数，也不使用评测标签。

五折、10 次重复的正式 benchmark 与原 validation gate 同口径比较如下：

| 协议 | K | 原 validation gate Gain | 可靠性收缩 Gain | 差值 |
|---|---:|---:|---:|---:|
| chronological | 10 | 0.0408° | 0.0465° | +0.0057° |
| chronological | 20 | 0.0431° | 0.0493° | +0.0062° |
| chronological | 50 | 0.0440° | 0.0519° | +0.0079° |
| interleaved | 10 | 0.0402° | 0.0427° | +0.0025° |
| interleaved | 20 | 0.0330° | 0.0377° | +0.0047° |
| interleaved | 50 | 0.0409° | 0.0496° | +0.0086° |

六个组合均提高平均 Gain。结果目录为：

`/data1/luxliang/gaze-personalization/runs/reliability-bias-cv5-r10-v3-aggregate-20260726`

该结论目前说明“参数重复性可用于改善补偿强度估计”。下一步仍需冻结公式，在新 checkpoint、设备或采集场景上做外部验证，不能继续依据当前五折结果调整公式常数。

## 7. 冻结公式的 checkpoint 外部验证

冻结第 6 节公式后，在无约束 checkpoint
`bdb6e6ba054eb5f1e0d1fcb436b2bdda2eb09ef76c43e97fd9f2a71fd8e741a5`
及固定 6 位留出用户上验证，未依据结果修改任何常数。

| 协议 | K | 可靠性收缩 Gain |
|---|---:|---:|
| chronological | 10 | -0.0535° |
| chronological | 20 | -0.0495° |
| chronological | 50 | -0.0517° |
| interleaved | 10 | -0.0293° |
| interleaved | 20 | -0.0389° |
| interleaved | 50 | -0.0322° |

六个配置全部失败，因此可靠性偏置不能被表述为跨 checkpoint 普适方案。机制诊断显示，无约束 checkpoint 的校准偏置幅值约为 0.55°–0.57°，而评测 oracle 偏置仅约 0.32°；校准内部估计虽然稳定，却系统性高估可迁移补偿量。这说明：

1. 分半可靠性只刻画校准抽样噪声；
2. 它不能观测 checkpoint/context 引起的偏置衰减；
3. “参数稳定”是个性化成立的必要条件，而非充分条件；
4. 方法结论必须限定为当前约束训练路径，直至新场景独立验证通过。

外部验证结果位于：

`/data1/luxliang/gaze-personalization/runs/reliability-bias-no-constraint-heldout6-r20-20260726`

项目新增研究发布准入检查：指定的每个独立验证套件、协议和 K 不仅要求平均 Gain 达到阈值，还要求逐用户配对 Gain 的确定性 bootstrap 单侧 95% 下界达到阈值，否则命令返回非零状态并生成失败表。

`/data1/luxliang/gaze-personalization/runs/reliability-bias-promotion-audit-20260726`

## 8. 自动化研究审计

`run_personalization_research_audit.py` 将以下步骤固定为同一条流水线：

1. 从每个验证套件的逐折 manifest/cache 重新计算机制指标；
2. 生成逐用户偏置运输分类和跨 checkpoint 失效归因；
3. 从各套件独立 summary 执行发布准入；
4. 记录代码 commit、Python/平台、输入 summary 与 manifest 哈希、输出哈希和唯一命令。

当前服务器配置保存在 `research_audit_config.json`，执行命令为：

```bash
python3 run_personalization_research_audit.py \
  --config research_audit_config.json \
  --output-dir /new/output/directory
```

审计状态为 FAIL 时程序返回 1，但仍完整写出归因、准入和复现产物。状态失败表示研究结论不满足发布条件，不表示流水线运行异常。

## 9. 多 checkpoint 冻结验证矩阵

在不修改可靠性公式的前提下，又加入 PIR single-strong、PIR single-weak、T4 和 PIR two-stage 四个兼容 checkpoint。所有实验沿用相同 6 位留出用户、相同协议和 K。

| checkpoint | chronological K=10/20/50 Gain | interleaved K=10/20/50 Gain | 结论 |
|---|---|---|---|
| constrained CV5 | +0.0465 / +0.0493 / +0.0519° | +0.0427 / +0.0377 / +0.0496° | 六项均值和 95% 下界通过 |
| no-constraint | -0.0535 / -0.0495 / -0.0517° | -0.0293 / -0.0389 / -0.0322° | 六项失败 |
| PIR single-strong | +0.0113 / +0.0020 / -0.0008° | +0.0600 / +0.0650 / +0.0675° | 正均值项的 95% 下界仍跨零 |
| PIR single-weak | +0.0287 / +0.0314 / +0.0349° | -0.0249 / -0.0365 / -0.0257° | 正均值项的 95% 下界仍跨零 |
| T4 | -0.0463 / -0.0492 / -0.0583° | +0.0335 / +0.0138 / +0.0129° | 正均值项的 95% 下界仍跨零 |
| PIR two-stage | -0.0405 / -0.0474 / -0.0488° | -0.0306 / -0.0351 / -0.0383° | 六项失败 |

由此得到更严格的研究结论：

1. 两参数偏置只在约束版 56 用户五折上获得跨用户稳定迁移证据；
2. checkpoint 的训练约束会改变残差的时间与方向结构；
3. 模型整体误差强弱不能预测个性化补偿是否有效；
4. 协议不能脱离 checkpoint 单独选择；
5. 在找到可观测的运输判据前，方案必须保持“checkpoint/protocol 条件有效”的限定。

按“平均 Gain 与单侧 95% 下界均不低于 0°”的准入规则，36 个配置中仅约束版五折的 6 个配置通过，其余 30 个配置失败。6 人套件中的正均值只能作为继续扩样本验证的信号，不能作为发布证据。

## 10. 校准内部信息的跨 checkpoint 可预测性

为检验运行时 gate 是否可能只依赖校准数据，使用以下五个上线时可观测特征：

- \(\log K\)；
- protocol；
- 偏置参数幅值；
- 分半参数不稳定度；
- 可靠性缩放系数。

先对同一用户、协议和 K 的重复实验取均值，再使用固定 \(10^{-3}\) 岭回归进行 leave-one-checkpoint-suite-out 验证。共包含 504 个逐用户配置、6 个验证套件。结果为：

| 指标 | 结果 |
|---|---:|
| 校准特征模型 RMSE | 0.1359° |
| 训练套件均值基线 RMSE | 0.1311° |
| Gain 符号准确率 | 35.71% |
| 预测 Gain 与真实 Gain 相关系数 | -0.205 |
| RMSE 优于基线的留出套件数 | 0 / 6 |

因此现有校准内部指标不具备跨 checkpoint 的迁移成败预测能力。不能把可靠性系数继续包装成普适运行时 gate。当前可证实的部署规则是：

1. 在目标 checkpoint 与目标协议上预先完成独立用户验证；
2. 未通过发布准入的 checkpoint/protocol 禁止启用个性化；
3. 单个用户的可靠性收缩只在已通过准入的组合内控制估计噪声；
4. 若要实现真正在线 gate，需要引入当前校准数据之外、能够观测 context transport 的信息。

## 11. 两参数偏置的分量可辨识性

为回答“两参数适配器究竟学到了什么”，固定每次校准已经拟合并经过可靠性收缩的
\([b_{\mathrm{yaw}},b_{\mathrm{pitch}}]\)，在同一隔离评测集上比较四种处理：

1. zero：不补偿；
2. yaw-only：\([b_{\mathrm{yaw}},0]\)；
3. pitch-only：\([0,b_{\mathrm{pitch}}]\)；
4. full：\([b_{\mathrm{yaw}},b_{\mathrm{pitch}}]\)。

该消融不重新拟合参数，也不使用评测标签做选择。统计先对同一用户的重复抽样取均值，
再对用户做宏平均；full 与单分量的差值使用逐用户配对 bootstrap。六套 checkpoint、
两种协议、K∈{10,20,50} 共形成 36 个配置、21,600 条分量重评估记录。full 重算与原始
结果的最大差为 0.0002534°。

| 分量或比较 | 均值为正的配置 | 单侧 95% 下界为正 | 36 配置宏平均 |
|---|---:|---:|---:|
| yaw-only | 3 / 36 | 0 / 36 | -0.0261° |
| pitch-only | 23 / 36 | 13 / 36 | +0.0202° |
| full | 17 / 36 | 6 / 36 | -0.0029° |
| full − yaw-only（加入 pitch） | 24 / 36 | 16 / 36 | +0.0233° |
| full − pitch-only（加入 yaw） | 6 / 36 | 0 / 36 | -0.0230° |

因此当前数据不支持“yaw/pitch 两个个性化参数均被可靠识别”。可迁移信号主要来自
pitch；yaw 分量跨 checkpoint 总体损害泛化。两参数 full 在约束版五折上有效，不能
解释为两个方向都实现了个性化，而应解释为 pitch 信号足以覆盖 yaw 噪声的条件性结果。

下一轮候选应是预先声明的一参数 pitch-only 补偿，并在新的独立用户或 checkpoint 上
完整重跑拟合、稳定性和发布准入。当前分量消融只能提出该假设，不能用同一批评测结果
直接把 pitch-only 宣布为已验证方案。

复现命令：

```bash
python3 analyze_bias_components.py \
  --config research_audit_config.json \
  --output-dir /path/to/bias-component-ablation
```

正式产物位于：

`/data1/luxliang/gaze-personalization/runs/bias-component-ablation-f2cb907`

## 12. 一参数 pitch-only 候选复验

依据分量消融预先定义 `pitch_bias`：唯一可训练参数为 pitch 角偏置，yaw 恒为零；可靠性
收缩使用 pitch 标量的分半差和标量幅值。沿用六套实验的 checkpoint、用户划分、协议、
K、重复次数和随机种子重新拟合，并与原两参数 bias 逐用户配对。

36 个配置中，pitch-only 有 22 个平均 Gain 为正、16 个单侧 95% bootstrap 下界为正；
相对两参数 bias 有 28 个平均改善、16 个改善下界为正。它显著减少了 yaw 噪声，但没有
消除场景依赖：PIR two-stage 仍退化，约束五折也存在退化 fold。因此该方案升级为
“更可辨识的候选”，尚未成为普适方案；必须在未参与本轮筛选的新用户/新 checkpoint
上冻结验证。

产物位于：

`/data1/luxliang/gaze-personalization/runs/pitch-bias-candidate-20260726`
